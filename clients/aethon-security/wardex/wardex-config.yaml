# Wardex Configuration — Aethon Security
# CRA Class II — stricter thresholds for SIEM/endpoint product

thresholds:
  critical_vulnerabilities: 0
  high_vulnerabilities: 0

output:
  format: "json"
  file: "wardex-report.json"
  roadmap_limit: 20

security:
  enforce_actor_trust: true
  fail_close_default: 1.0
